version: "2.0"

intents:
  - saudar
  - tchau
  - afirmacao
  - negacao
  - filmes

actions:
  - action_procurar_filme

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
